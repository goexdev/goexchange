# goexchange-public

# Build
bin/
*.exe
*.dll
*.so
*.dylib

# Test
*.test
*.out
coverage.txt

# Go
vendor/
.idea/
.vscode/
*.iml

# Logs
*.log
logs/

# Local config
config.local.yaml
.env.local

# OS
.DS_Store
Thumbs.db

# Runtime
tmp/
*.tmp
web/node_modules/
web/dist/
web/.vite/
web/tsconfig.tsbuildinfo
web/.timestamp-*/
/uploads/

# Reports (regenerated by CI)
reports/

# QA artifacts
e2e/screenshots/
e2e/reports/
web/e2e/test-results/
web/e2e/report/
tests/screenshots/

# Docker build artifacts
Dockerfile.bak

# Internal documentation: kept locally only, never pushed.
# The detailed design / runbook / audit docs live in the operator's
# private archive (see /root/docs-archive/ on the maintainer's machine).
# Adding new files here is intentionally allowed so devs can keep their
# own notes; the rule below prevents them from being committed.
docs/
docs/**/